{
  "scripts": {
    "server-dev": "npx nodemon --watch server index.js"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "morgan": "^1.10.0",
    "neo4j-driver": "^5.2.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
